# CS League of Legends Counter
Experimental project to apply a custom slider. You can slide between 0 and 60 min games to see how many cs have spawned on a single lane.

# Running the project
To run this project, you node (22.13+), run `npm install -g yarn`, then in the project `yarn install` to install packages. 
`yarn start` to run the project.